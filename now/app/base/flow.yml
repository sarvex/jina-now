jtype: Flow
version: '1'
with:
  name: nowapi
  monitoring: true
  timeout_send: ${{ ENV.TIMEOUT_SEND }}
  env:
    JINA_LOG_LEVEL: DEBUG
jcloud:
  version: ${{ ENV.JINA_VERSION }}
  retention_days: ${{ RETENTION_DAYS }}
  custom_dns:
    - ${{ ENV.CUSTOM_DNS }}
executors:
