jtype: Flow
version: '1'
with:
  name: nowapi
  monitoring: true
  env:
    JINA_LOG_LEVEL: DEBUG
    JINA_OPTOUT_TELEMETRY: true
jcloud:
  version: ${{ ENV.JINA_VERSION }}
  retention_days: ${{ RETENTION_DAYS }}
  custom_dns:
    - ${{ ENV.CUSTOM_DNS }}
executors:
