jtype: Flow
version: '1'
with:
  name: nowapi
  monitoring: true
  env:
    JINA_LOG_LEVEL: DEBUG
    JINA_OPTOUT_TELEMETRY: ${{ ENV.NOW_CI_RUN }}
jcloud:
  version: ${{ ENV.JINA_VERSION }}
  custom_dns:
    - ${{ ENV.CUSTOM_DNS }}
executors:
